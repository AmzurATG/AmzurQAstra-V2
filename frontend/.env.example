# Frontend Environment Configuration
# Copy to .env and update values

# API URL (proxied through Vite in development)
VITE_API_URL=/api/v1

# Direct API URL (if not using Vite proxy)
# VITE_API_URL=http://localhost:8000/api/v1
